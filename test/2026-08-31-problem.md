# Python Basic Quiz 50문제

- 시험일: 2026-08-31
- 과목: Python Basic Quiz
- 총 문항: 50문제

## 01. 변수
다음 코드 실행 후 `name` 변수에 저장된 값은 무엇인가요?
```python
name = "Python"
name = "AI"
```
- A. Python
- B. AI
- C. name
- D. 오류가 발생한다

## 02. 자료형
다음 중 정수(int) 자료형에 해당하는 값은 무엇인가요?
- A. `"10"`
- B. `10`
- C. `10.0`
- D. `True`

## 03. 출력
다음 코드의 출력 결과로 알맞은 것은 무엇인가요?
```python
print("Hello", "Python")
```
- A. HelloPython
- B. Hello Python
- C. "Hello", "Python"
- D. 오류가 발생한다

## 04. 문자열
문자열 `"Python"`의 길이를 구하는 코드로 알맞은 것은 무엇인가요?
- A. `length("Python")`
- B. `len("Python")`
- C. `count("Python")`
- D. `size("Python")`

## 05. 형변환
사용자에게 입력받은 문자열 `"25"`를 정수로 바꾸는 코드로 알맞은 것은 무엇인가요?
- A. `str("25")`
- B. `int("25")`
- C. `float = "25"`
- D. `input(25)`

## 06. 연산자
다음 코드의 출력 결과는 무엇인가요?
```python
print(7 // 2)
print(7 % 2)
```
- A. 3 과 1
- B. 3.5 와 0
- C. 1 과 3
- D. 7 과 2

## 07. 조건문
`score`가 80 이상일 때만 "합격"을 출력하려고 합니다. 빈칸에 들어갈 코드는?
```python
score = 85
if ________:
    print("합격")
```
- A. `score = 80`
- B. `score >= 80`
- C. `score => 80`
- D. `score == "80"`

## 08. 조건문
다음 코드의 출력 결과는 무엇인가요?
```python
age = 17
if age >= 20:
    print("성인")
else:
    print("미성년")
```
- A. 성인
- B. 미성년
- C. age
- D. 아무것도 출력되지 않는다

## 09. 조건문
`if / elif / else`에 대한 설명으로 가장 알맞은 것은 무엇인가요?
- A. 모든 조건을 반드시 실행한다
- B. 위에서부터 조건을 검사하고 처음 참인 블록만 실행한다
- C. else는 조건식이 반드시 필요하다
- D. elif는 반복문에서만 사용한다

## 10. 논리연산
x가 10 이상이고 20 이하인지 확인하는 조건식으로 알맞은 것은 무엇인가요?
- A. `x >= 10 and x <= 20`
- B. `x >= 10 or x <= 20`
- C. `x => 10 and x =< 20`
- D. `10 >= x >= 20`

## 11. 반복문
다음 코드는 몇 번 출력되나요?
```python
for i in range(5):
    print("Python")
```
- A. 4번
- B. 5번
- C. 6번
- D. 무한 반복

## 12. 반복문
다음 코드의 출력 결과로 알맞은 것은 무엇인가요?
```python
for i in range(1, 4):
    print(i)
```
- A. 1 2 3
- B. 1 2 3 4
- C. 0 1 2 3
- D. 4만 출력된다

## 13. 누적합
1부터 5까지의 합을 `total`에 누적하려고 합니다. 빈칸에 들어갈 코드는?
```python
total = 0
for i in range(1, 6):
    ________
print(total)
```
- A. `total = i`
- B. `total += i`
- C. `i += total`
- D. `total == i`

## 14. while
다음 코드의 출력 결과는 무엇인가요?
```python
count = 1
while count <= 3:
    print(count)
    count += 1
```
- A. 1 2 3
- B. 1 2 3 4
- C. 0 1 2
- D. 무한 반복

## 15. break
반복문을 즉시 종료할 때 사용하는 명령어는 무엇인가요?
- A. stop
- B. exit
- C. break
- D. continue

## 16. continue
다음 코드의 출력 결과로 알맞은 것은 무엇인가요?
```python
for i in range(1, 5):
    if i == 3:
        continue
    print(i)
```
- A. 1 2 3 4
- B. 1 2 4
- C. 3만 출력
- D. 아무것도 출력되지 않음

## 17. 리스트
리스트 `numbers`의 첫 번째 값을 출력하는 코드로 알맞은 것은 무엇인가요?
```python
numbers = [10, 20, 30]
```
- A. `numbers[0]`
- B. `numbers[1]`
- C. `numbers.first()`
- D. `numbers(0)`

## 18. 리스트
다음 코드의 출력 결과는 무엇인가요?
```python
fruits = ["사과", "바나나"]
fruits.append("포도")
print(fruits)
```
- A. ["포도"]
- B. ["사과", "바나나"]
- C. ["사과", "바나나", "포도"]
- D. 오류가 발생한다

## 19. 리스트
리스트의 마지막 요소를 가져오는 코드로 알맞은 것은 무엇인가요?
```python
items = ["A", "B", "C"]
```
- A. `items[3]`
- B. `items[-1]`
- C. `items[last]`
- D. `items.end()`

## 20. 리스트 반복
리스트 안의 값을 하나씩 꺼내 출력하는 코드로 가장 알맞은 것은 무엇인가요?
```python
fruits = ["사과", "바나나", "포도"]
```
- A. `for fruit in fruits:` 후 `print(fruit)`
- B. `for fruits in fruit:` 후 `print(fruit)`
- C. `print(fruits[3])`
- D. `repeat fruits`

## 21. 리스트 중복 제거
set을 쓰지 않고 중복 없는 리스트를 만들 때 핵심 조건식으로 알맞은 것은 무엇인가요?
```python
tags = ["Python", "AI", "Python"]
unique_tags = []
```
- A. `if tag not in unique_tags:`
- B. `if tag in tags:`
- C. `if tag == unique_tags:`
- D. `if tags not tag:`

## 22. 리스트 길이
다음 코드의 출력 결과는 무엇인가요?
```python
scores = [80, 90, 100]
print(len(scores))
```
- A. 2
- B. 3
- C. 80
- D. 270

## 23. 인덱스
`range`와 `len`을 함께 사용하는 이유로 가장 알맞은 것은 무엇인가요?
- A. 리스트 길이가 바뀌어도 반복 횟수를 자동으로 맞추기 위해
- B. 코드를 더 느리게 만들기 위해
- C. 문자열만 반복하기 위해
- D. print를 생략하기 위해

## 24. 딕셔너리
다음 딕셔너리에서 `"name"`에 해당하는 값을 출력하는 코드는 무엇인가요?
```python
student = {"name": "민수", "age": 20}
```
- A. `student["name"]`
- B. `student.name`
- C. `student(0)`
- D. `student["민수"]`

## 25. 딕셔너리
다음 코드의 출력 결과는 무엇인가요?
```python
count = {}
count["apple"] = 1
count["apple"] += 1
print(count["apple"])
```
- A. 1
- B. 2
- C. apple
- D. 오류가 발생한다

## 26. 딕셔너리
딕셔너리에 특정 key가 이미 있는지 확인하는 조건식으로 알맞은 것은 무엇인가요?
```python
word_count = {"사과": 2}
word = "사과"
```
- A. `if word in word_count:`
- B. `if word_count in word:`
- C. `if word == 2:`
- D. `if word_count[word] = word:`

## 27. 단어 빈도
문장을 단어 단위로 나누기 위해 사용하는 코드로 알맞은 것은 무엇인가요?
```python
sentence = "사과 바나나 사과"
```
- A. `sentence.split()`
- B. `sentence.cut()`
- C. `split(sentence)`
- D. `sentence.list()`

## 28. 단어 빈도
다음 코드 실행 후 `word_count`의 내용으로 알맞은 것은 무엇인가요?
```python
words = ["사과", "바나나", "사과"]
word_count = {}
for word in words:
    if word in word_count:
        word_count[word] += 1
    else:
        word_count[word] = 1
print(word_count)
```
- A. {"사과": 2, "바나나": 1}
- B. {"사과": 1, "바나나": 1}
- C. ["사과", "바나나"]
- D. 오류가 발생한다

## 29. 함수
함수를 정의할 때 사용하는 키워드는 무엇인가요?
- A. function
- B. def
- C. make
- D. class

## 30. 함수
다음 코드의 출력 결과는 무엇인가요?
```python
def add(a, b):
    return a + b

print(add(3, 4))
```
- A. 3
- B. 4
- C. 7
- D. add

## 31. 함수
`return`의 역할로 가장 알맞은 것은 무엇인가요?
- A. 함수를 반복 실행한다
- B. 함수의 결과값을 밖으로 돌려준다
- C. 화면을 지운다
- D. 오류를 무시한다

## 32. 함수
다음 함수 호출 결과로 알맞은 것은 무엇인가요?
```python
def greet(name):
    print(name + "님 안녕하세요")

greet("수진")
```
- A. 수진님 안녕하세요
- B. name님 안녕하세요
- C. greet님 안녕하세요
- D. 아무것도 출력되지 않음

## 33. 모듈
`math` 모듈을 사용하기 위해 먼저 작성해야 하는 코드는 무엇인가요?
- A. use math
- B. import math
- C. open math
- D. module math

## 34. 모듈
다음 코드의 출력 결과는 무엇인가요?
```python
import math
print(math.sqrt(81))
```
- A. 8
- B. 9.0
- C. 81
- D. 오류가 발생한다

## 35. 파일
파일을 읽기 모드로 열 때 사용하는 모드 문자열은 무엇인가요?
- A. `"r"`
- B. `"w"`
- C. `"a"`
- D. `"x"`

## 36. 파일
파일을 새로 쓰기 모드로 열 때 주의할 점으로 알맞은 것은 무엇인가요?
- A. 기존 내용이 지워질 수 있다
- B. 반드시 이미지 파일만 가능하다
- C. 숫자만 저장할 수 있다
- D. 파일을 닫을 수 없다

## 37. 파일
`with open(...) as f:` 구문을 사용하는 장점은 무엇인가요?
- A. 파일을 자동으로 닫아준다
- B. 반복문을 자동으로 만든다
- C. 문자열을 숫자로 바꾼다
- D. 모든 오류를 없앤다

## 38. 예외처리
오류가 발생할 수 있는 코드를 감싸는 키워드는 무엇인가요?
- A. try
- B. error
- C. check
- D. finally

## 39. 예외처리
정수로 바꿀 수 없는 값을 `int()`로 변환할 때 주로 발생하는 예외는 무엇인가요?
- A. ValueError
- B. ZeroDivisionError
- C. NameError
- D. TypeError

## 40. 예외처리
다음 코드의 출력 결과는 무엇인가요?
```python
try:
    number = int("abc")
except ValueError:
    print("정수를 입력하세요")
```
- A. abc
- B. 정수를 입력하세요
- C. 0
- D. 아무것도 출력되지 않음

## 41. 예외처리
`try` 구문에서 오류가 발생하지 않았을 때 실행되는 블록은 무엇인가요?
- A. except
- B. else
- C. finally
- D. error

## 42. 예외처리
오류 발생 여부와 상관없이 마지막에 실행되는 블록은 무엇인가요?
- A. try
- B. except
- C. else
- D. finally

## 43. 코드 읽기
다음 코드의 출력 결과로 알맞은 것은 무엇인가요?
```python
numbers = [1, 2, 3, 4]
result = []
for n in numbers:
    if n % 2 == 0:
        result.append(n)
print(result)
```
- A. [1, 3]
- B. [2, 4]
- C. [1, 2, 3, 4]
- D. []

## 44. 코드 읽기
다음 코드의 출력 결과는 무엇인가요?
```python
data = {"A": 3, "B": 1}
for key in data:
    if data[key] >= 2:
        print(key)
```
- A. A
- B. B
- C. A와 B
- D. 3

## 45. 코드 수정
다음 코드가 오류 없이 동작하려면 어느 부분을 고쳐야 할까요?
```python
fruits = ["사과", "바나나"]
print(fruits[2])
```
- A. `fruits[2]`를 `fruits[1]`로 바꾼다
- B. print를 삭제한다
- C. 리스트를 딕셔너리로 바꾼다
- D. "사과"를 숫자로 바꾼다

## 46. 코드 수정
다음 코드의 문제점으로 가장 알맞은 것은 무엇인가요?
```python
age = input("나이: ")
if age >= 20:
    print("성인")
```
- A. input 결과는 문자열이므로 int 변환이 필요하다
- B. if는 사용할 수 없다
- C. print 문법이 틀렸다
- D. age 변수명은 사용할 수 없다

## 47. 알고리즘 사고
중복 없는 태그 리스트를 만드는 순서로 가장 알맞은 것은 무엇인가요?
- A. 빈 리스트 생성 → 원본 리스트 반복 → 없으면 추가
- B. 출력 먼저 하기 → 리스트 삭제 → 반복 종료
- C. 원본 리스트 삭제 → 빈 리스트 출력
- D. 같은 값만 무조건 추가

## 48. 알고리즘 사고
문제 해결을 시작할 때 가장 먼저 하면 좋은 일은 무엇인가요?
- A. 바로 복잡한 코드를 작성한다
- B. 입력, 처리, 출력을 글로 먼저 정리한다
- C. 오류 메시지를 무시한다
- D. 무조건 AI가 만든 코드를 복사한다

## 49. AI 활용
Python 학습에서 AI를 가장 바람직하게 활용하는 방법은 무엇인가요?
- A. 정답 코드만 받아서 그대로 제출한다
- B. 힌트, 설명, 오류 원인 확인에 활용하고 직접 수정한다
- C. 코드 실행은 하지 않는다
- D. 변수명을 모두 AI에게 맡긴다

## 50. 종합
아래 코드의 목적을 가장 잘 설명한 것은 무엇인가요?
```python
unique_tags = []
for tag in tags:
    if tag not in unique_tags:
        unique_tags.append(tag)
```
- A. tags의 모든 값을 삭제한다
- B. tags에서 중복을 제거해 unique_tags에 저장한다
- C. tags의 길이를 출력한다
- D. tags를 문자열로 바꾼다
