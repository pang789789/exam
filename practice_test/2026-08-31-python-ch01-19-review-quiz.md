# Python Chapter 01~19 복습 퀴즈

- 시험일: 2026-08-31
- 구성: A형 38문제 + B형 38문제
- 단답형 + 다중선택형
- 총 76문제

> 원본 문제지의 문제 순서와 표현을 기준으로 정리했습니다. 정답/해설은 포함하지 않습니다.

---

# A형

## Chapter 01. AI 시대에는 파이썬을 어떻게 배워야 할까?

### 1. [단답형]
프로그래밍을 초보자 관점에서 한 문장으로 설명할 때, 이 강의에서 강조한 핵심 표현을 쓰시오.

**답:**

### 2. [다중선택]
다음 중 AI 시대에 사람이 직접 담당해야 할 역할을 모두 고르시오.

- ① 무엇을 해결할지 결정한다.
- ② AI가 만든 코드의 결과가 맞는지 판단한다.
- ③ 모든 코드를 AI가 작성하도록 맡긴다.
- ④ 오류 원인을 확인하고 최종 결정을 내린다.
- ⑤ 이해하지 못한 코드를 실행하지 않고 제출한다.

**선택:**

## Chapter 02. 파이썬 개발 환경 준비하기

### 3. [단답형]
VS Code에서 작성한 파이썬 코드를 실제로 읽고 실행하는 프로그램을 무엇이라고 하는가?

**답:**

### 4. [다중선택]
`python --version`이 정상 동작하지 않을 때의 점검 방법으로 적절한 것을 모두 고르시오.

- ① 현재 터미널의 오류 메시지를 확인한다.
- ② Windows에서 `py --version`도 확인해 본다.
- ③ 무조건 운영체제를 재설치한다.
- ④ Python 설치 후 새 터미널을 열어 다시 확인한다.
- ⑤ VS Code만 설치되어 있으면 Python 설치 여부는 확인할 필요가 없다.

**선택:**

## Chapter 03. 생성형 AI에게 코딩을 질문하는 방법

### 5. [단답형]
AI에게 오류 해결을 질문할 때 코드와 함께 반드시 전달해야 할 핵심 정보 중 하나로, 실제로 화면에 나타난 오류 내용을 무엇이라고 하는가?

**답:**

### 6. [다중선택]
학습용 코딩 질문을 더 정확하게 만드는 정보로 적절한 것을 모두 고르시오.

- ① 나의 현재 수준
- ② 구현하려는 목표
- ③ 실제 코드와 실행 결과
- ④ 원하는 도움의 형태
- ⑤ API 키와 비밀번호 원문

**선택:**

## Chapter 04. 처음 만나는 파이썬

### 7. [단답형]
다음 코드의 출력 결과를 그대로 쓰시오.

```python
print("Python", 100)
```

**답:**

### 8. [다중선택]
다음 중 Chapter 04에서 배운 파이썬 기본 규칙으로 맞는 것을 모두 고르시오.

- ① 문자열은 따옴표로 감쌀 수 있다.
- ② `#` 뒤의 내용은 주석으로 사용할 수 있다.
- ③ 파이썬 코드는 기본적으로 위에서 아래로 실행된다.
- ④ 들여쓰기는 언제나 보기 좋게 만드는 장식일 뿐이다.
- ⑤ `print()` 안에 여러 값을 쉼표로 나누어 넣을 수 있다.

**선택:**

## Chapter 05. 변수와 데이터형

### 9. [단답형]
다음 값의 데이터형 이름을 쓰시오.

```python
age = "20"
```

**답:**

### 10. [다중선택]
다음 중 변수와 데이터형에 대한 설명으로 맞는 것을 모두 고르시오.

- ① `score = 80` 뒤에 `score = 95`를 실행하면 현재 score 값은 95이다.
- ② `type()`으로 값의 데이터형을 확인할 수 있다.
- ③ `int("25")`는 정수 25로 변환할 수 있다.
- ④ `int("스물")`은 정상적으로 정수 20이 된다.
- ⑤ 변수 이름에는 값의 의미가 드러나도록 작성하는 것이 좋다.

**선택:**

## Chapter 06. 연산자로 계산하기

### 11. [단답형]
다음 두 식의 결과를 순서대로 쓰시오.

```python
17 // 5
17 % 5
```

**답:**

### 12. [다중선택]
다음 중 연산 결과가 올바른 것을 모두 고르시오.

- ① `2 ** 3` → 8
- ② `10 / 2` → 5.0
- ③ `17 % 5` → 2
- ④ `3 > 5` → True
- ⑤ `10 != 7` → True

**선택:**

## Chapter 07. 문자열 다루기

### 13. [단답형]
다음 코드의 결과를 쓰시오.

```python
text = "Python"
print(text[1:4])
```

**답:**

### 14. [다중선택]
다음 중 문자열 처리에 사용할 수 있는 기능을 모두 고르시오.

- ① `strip()`로 앞뒤 공백 제거
- ② `replace()`로 일부 문자열 치환
- ③ `upper()`로 대문자 변환
- ④ `append()`로 문자열 끝에 문자 추가
- ⑤ `len()`으로 문자열 길이 확인

**선택:**

## Chapter 08. 사용자에게 데이터 입력받기

### 15. [단답형]
`input()`으로 입력받은 값의 기본 데이터형을 쓰시오.

**답:**

### 16. [다중선택]
사용자 입력을 이용한 프로그램에 대한 설명으로 맞는 것을 모두 고르시오.

- ① 숫자 계산이 필요하면 `int()`나 `float()` 변환을 고려한다.
- ② `strip()`으로 입력 문자열의 앞뒤 공백을 정리할 수 있다.
- ③ `input()`은 사용자가 값을 입력할 때까지 기다린다.
- ④ `input()`은 숫자를 입력하면 자동으로 int를 반환한다.
- ⑤ 숫자로 바꿀 수 없는 문자열을 `int()`로 변환하면 ValueError가 발생할 수 있다.

**선택:**

## Chapter 09. 조건에 따라 실행하기

### 17. [단답형]
다음 코드에서 출력되는 문장을 쓰시오.

```python
score = 60
if score >= 60:
    print("통과")
```

**답:**

### 18. [다중선택]
조건문에 대한 설명으로 맞는 것을 모두 고르시오.

- ① `if`의 조건식이 True일 때 들여쓰기된 코드가 실행된다.
- ② `if` 뒤에는 콜론(`:`)이 필요하다.
- ③ `=`와 `==`는 같은 의미이다.
- ④ `elif`를 사용해 여러 조건을 순서대로 검사할 수 있다.
- ⑤ 조건이 겹치면 조건의 배치 순서가 결과에 영향을 줄 수 있다.

**선택:**

## Chapter 10. 반복해서 실행하기

### 19. [단답형]
다음 코드에서 `print(i)`는 몇 번 실행되는가?

```python
for i in range(5):
    print(i)
```

**답:**

### 20. [다중선택]
반복문에 대한 설명으로 맞는 것을 모두 고르시오.

- ① `range(5)`는 0,1,2,3,4를 만든다.
- ② `break`는 반복문을 즉시 종료할 수 있다.
- ③ `continue`는 현재 반복의 남은 부분을 건너뛰고 다음 반복으로 진행한다.
- ④ `range(0)`은 항상 오류가 발생한다.
- ⑤ `while`문은 조건과 상태 변화가 중요하다.

**선택:**

## Chapter 11. 리스트로 여러 데이터 관리하기

### 21. [단답형]
다음 리스트에서 마지막 값을 인덱싱하는 표현을 하나 쓰시오.

```python
fruits = ["사과", "바나나", "포도"]
```

**답:**

### 22. [다중선택]
리스트에 대한 설명으로 맞는 것을 모두 고르시오.

- ① `append()`는 리스트 끝에 값을 추가할 수 있다.
- ② `sum()`은 숫자 리스트의 합계를 구할 수 있다.
- ③ 리스트의 첫 인덱스는 1이다.
- ④ 리스트의 특정 요소는 수정할 수 있다.
- ⑤ `sort()`로 리스트의 순서를 정렬할 수 있다.

**선택:**

## Chapter 12. 튜플과 집합

### 23. [단답형]
요소가 하나인 튜플 `(10,)`에서 튜플임을 결정하는 핵심 기호를 쓰시오.

**답:**

### 24. [다중선택]
튜플과 집합에 대한 설명으로 맞는 것을 모두 고르시오.

- ① 튜플은 순서가 있다.
- ② 튜플 요소는 직접 수정할 수 없다.
- ③ 집합은 중복 값을 제거하는 특징이 있다.
- ④ 빈 집합은 `{}`로 만드는 것이 가장 정확하다.
- ⑤ 집합에서는 합집합·교집합·차집합을 사용할 수 있다.

**선택:**

## Chapter 13. 딕셔너리로 키와 값 관리하기

### 25. [단답형]
다음 딕셔너리에서 점수 85를 가져오는 표현을 쓰시오.

```python
student = {"name": "민수", "score": 85}
```

**답:**

### 26. [다중선택]
딕셔너리에 대한 설명으로 맞는 것을 모두 고르시오.

- ① 값은 키를 이용해 조회할 수 있다.
- ② `get()`은 없는 키를 비교적 안전하게 다루는 데 활용할 수 있다.
- ③ `items()`는 키와 값을 함께 반복 처리할 때 사용할 수 있다.
- ④ 딕셔너리는 반드시 숫자 인덱스로만 값을 찾아야 한다.
- ⑤ `in`으로 키 존재 여부를 확인할 수 있다.

**선택:**

## Chapter 14. 함수로 코드를 묶고 재사용하기

### 27. [단답형]
함수에서 계산 결과를 호출한 곳으로 돌려보낼 때 사용하는 키워드를 쓰시오.

**답:**

### 28. [다중선택]
함수에 대한 설명으로 맞는 것을 모두 고르시오.

- ① 함수는 `def`로 정의할 수 있다.
- ② 함수 정의만 해도 함수 본문이 즉시 실행된다.
- ③ 매개변수는 함수 정의에서 입력 자리를 나타낸다.
- ④ `return`과 `print()`는 항상 같은 역할이다.
- ⑤ 긴 코드를 역할별 작은 함수로 나누면 구조를 이해하기 쉬워질 수 있다.

**선택:**

## Chapter 15. 모듈·패키지와 가상환경

### 29. [단답형]
프로젝트마다 독립된 Python 실행 환경을 만들 때 사용하는 표준 기능 이름을 쓰시오.

**답:**

### 30. [다중선택]
모듈·패키지·가상환경에 대한 설명으로 맞는 것을 모두 고르시오.

- ① 모듈은 파이썬 코드를 담은 `.py` 파일이다.
- ② 패키지는 관련 모듈을 폴더 구조로 묶을 수 있다.
- ③ 표준 라이브러리는 일반적으로 Python과 함께 제공된다.
- ④ 가상환경을 만들면 모든 프로젝트가 반드시 같은 패키지 버전을 사용한다.
- ⑤ `requirements.txt`로 필요한 패키지와 버전을 기록할 수 있다.

**선택:**

## Chapter 16. 파일과 CSV 데이터 다루기

### 31. [단답형]
파일을 읽기 전용으로 열 때 사용하는 파일 모드 문자를 쓰시오.

**답:**

### 32. [다중선택]
파일과 CSV 처리에 대한 설명으로 맞는 것을 모두 고르시오.

- ① `with open()` 블록이 끝나면 파일이 자동으로 닫힌다.
- ② `read()`는 파일 전체 내용을 한 번에 읽을 수 있다.
- ③ CSV에서 읽은 숫자 데이터는 필요하면 int/float로 변환한다.
- ④ `w` 모드는 기존 내용 뒤에 항상 이어 쓰기만 한다.
- ⑤ `csv.DictReader`는 열 이름을 키처럼 다루는 데 사용할 수 있다.

**선택:**

## Chapter 17. 오류와 예외 처리

### 33. [단답형]
`int("abc")` 실행 시 발생하는 대표적인 예외 이름을 쓰시오.

**답:**

### 34. [다중선택]
오류와 예외 처리에 대한 설명으로 맞는 것을 모두 고르시오.

- ① 트레이스백은 마지막 줄의 예외 이름부터 확인하는 습관이 유용하다.
- ② `try-except`는 모든 논리 오류를 자동으로 수정한다.
- ③ 예외 종류별로 여러 `except`를 작성할 수 있다.
- ④ `finally`는 정리 작업 등에 활용할 수 있다.
- ⑤ 예외 이름 없는 `except:`를 습관적으로 넓게 쓰는 것은 권장되지 않는다.

**선택:**

## Chapter 18. 클래스와 객체 기초

### 35. [단답형]
클래스를 이용해 실제로 만든 값을 무엇이라고 하는가?

**답:**

### 36. [다중선택]
클래스와 객체에 대한 설명으로 맞는 것을 모두 고르시오.

- ① 클래스는 데이터와 기능의 구조를 정의하는 설계도처럼 볼 수 있다.
- ② `Student()`를 두 번 호출하면 항상 같은 객체가 반환된다.
- ③ `self`는 현재 객체 자신을 가리킨다.
- ④ `__init__()`은 객체 생성 시 초기값을 저장하는 데 사용할 수 있다.
- ⑤ 단순한 데이터 문제도 언제나 딕셔너리보다 클래스가 더 좋다.

**선택:**

## Chapter 19. AI와 함께 프로그램 설계하고 작성하기

### 37. [단답형]
프로그램을 설계할 때 Input, Process, Output을 묶어 부르는 약어를 쓰시오.

**답:**

### 38. [다중선택]
AI와 함께 프로그램을 설계할 때 적절한 행동을 모두 고르시오.

- ① 코드 작성 전에 목적과 범위를 정리한다.
- ② 이번에 하지 않을 기능도 명시할 수 있다.
- ③ 데이터의 모양을 리스트와 딕셔너리 등으로 먼저 생각한다.
- ④ 처음부터 전체 프로그램을 AI에게 맡기고 결과만 복사한다.
- ⑤ 작은 테스트 데이터의 예상 결과와 실제 결과를 비교한다.

**선택:**

---

# B형

## Chapter 01. AI 시대에는 파이썬을 어떻게 배워야 할까?

### 39. [단답형]
AI 시대의 파이썬 학습 목표를 ‘코드를 ○○하고 ○○하고 ○○하고 ○○하는 능력’이라는 네 동사로 쓰시오.

**답:**

### 40. [다중선택]
다음 중 AI를 ‘개인 튜터’처럼 사용하는 방식으로 적절한 것을 모두 고르시오.

- ① 어려운 개념을 더 쉽게 설명해 달라고 한다.
- ② 오류 메시지의 의미를 설명해 달라고 한다.
- ③ 문제를 읽자마자 전체 완성 코드를 요청한다.
- ④ 다음 한 단계의 힌트만 요청한다.
- ⑤ AI 답변을 실행하지 않고 정답으로 확정한다.

**선택:**

## Chapter 02. 파이썬 개발 환경 준비하기

### 41. [단답형]
터미널에서 설치된 Python 버전을 확인하는 대표 명령을 쓰시오.

**답:**

### 42. [다중선택]
파이썬 개발 환경 구성 요소와 역할의 연결로 맞는 것을 모두 고르시오.

- ① VS Code - 코드 작성과 파일 관리
- ② Python 실행기 - `.py` 코드 실행
- ③ Python 확장 - VS Code와 Python 사용을 편리하게 연결
- ④ Terminal - 명령 실행과 결과 확인
- ⑤ Explorer - Python 패키지를 인터넷에서 자동 설치

**선택:**

## Chapter 03. 생성형 AI에게 코딩을 질문하는 방법

### 43. [단답형]
AI에게 질문하기 전에 제거해야 하는 대표적인 민감정보를 한 가지 쓰시오.

**답:**

### 44. [다중선택]
다음 중 ‘정답 코드보다 학습을 돕는 질문’에 해당하는 것을 모두 고르시오.

- ① 왜 이 오류가 생겼는지 먼저 설명해 줘.
- ② 완성 코드는 주지 말고 첫 번째 힌트만 알려 줘.
- ③ 내 코드와 오류 메시지를 보고 원인을 단계별로 설명해 줘.
- ④ 아무 설명 없이 최종 정답 코드만 줘.
- ⑤ 내 수준에 맞게 어려운 문법은 피해서 설명해 줘.

**선택:**

## Chapter 04. 처음 만나는 파이썬

### 45. [단답형]
파이썬에서 한 줄 주석을 시작할 때 사용하는 기호를 쓰시오.

**답:**

### 46. [다중선택]
다음 코드에서 오류를 만들 가능성이 있는 변경을 모두 고르시오.

```python
print("Hello, Python!")
```

- ① 닫는 괄호 `)`를 삭제한다.
- ② 시작 큰따옴표만 삭제한다.
- ③ 문자열 내용을 `Hi`로 바꾼다.
- ④ `print`를 그대로 두고 문자열만 한글로 바꾼다.
- ⑤ 괄호 안 문자열의 끝 따옴표만 삭제한다.

**선택:**

## Chapter 05. 변수와 데이터형

### 47. [단답형]
`int("3.14")`을 그대로 실행하면 발생하는 대표적인 예외 이름을 쓰시오.

**답:**

### 48. [다중선택]
다음 중 실행 후 데이터형 설명이 맞는 것을 모두 고르시오.

- ① `int("25")` → int
- ② `float("3.5")` → float
- ③ `str(100)` → str
- ④ `bool(0)` → bool
- ⑤ `int("hello")` → int

**선택:**

## Chapter 06. 연산자로 계산하기

### 49. [단답형]
다음 식의 결과를 쓰시오.

```python
2 ** 3 + 1
```

**답:**

### 50. [다중선택]
다음 중 비교 또는 논리 연산의 결과가 True인 것을 모두 고르시오.

- ① `10 >= 10`
- ② `5 != 5`
- ③ `3 < 7 and 2 < 4`
- ④ `3 > 7 or 10 == 10`
- ⑤ `not True`

**선택:**

## Chapter 07. 문자열 다루기

### 51. [단답형]
다음 코드의 결과를 쓰시오.

```python
word = "Python"
print(word[-1])
```

**답:**

### 52. [다중선택]
다음 중 문자열 처리 코드로 올바른 것을 모두 고르시오.

- ① `" python ".strip()`은 앞뒤 공백을 제거한다.
- ② `"Python".lower()`는 소문자 문자열을 만든다.
- ③ `"banana".count("a")`는 특정 문자의 개수를 셀 수 있다.
- ④ 존재하지 않는 인덱스를 사용하면 IndexError가 날 수 있다.
- ⑤ 문자열의 한 글자는 리스트처럼 직접 대입해 수정할 수 있다.

**선택:**

## Chapter 08. 사용자에게 데이터 입력받기

### 53. [단답형]
사용자가 `12`를 입력했다고 할 때 다음 코드의 출력값을 쓰시오.

```python
age = int(input("나이: "))
print(age + 1)
```

**답:**

### 54. [다중선택]
다음 중 `input()` 사용 시 발생 가능한 문제와 대응으로 맞는 것을 모두 고르시오.

- ① 숫자 계산 전에 필요한 경우 형 변환한다.
- ② 앞뒤 공백이 문제라면 `strip()`을 사용할 수 있다.
- ③ `int("abc")`는 ValueError가 날 수 있다.
- ④ `input()` 반환값은 항상 float이므로 int 변환은 불필요하다.
- ⑤ 여러 번 input()을 호출해 서로 다른 변수에 저장할 수 있다.

**선택:**

## Chapter 09. 조건에 따라 실행하기

### 55. [단답형]
다음 코드에서 출력되는 문장을 쓰시오.

```python
score = 75
if score >= 90:
    print("A")
elif score >= 75:
    print("B")
else:
    print("C")
```

**답:**

### 56. [다중선택]
조건문을 안전하게 작성하는 방법으로 맞는 것을 모두 고르시오.

- ① 조건식의 경계값을 직접 테스트한다.
- ② 겹치는 조건이 있으면 검사 순서를 확인한다.
- ③ 조건 비교에는 항상 `=`를 사용한다.
- ④ 복합 조건에는 `and`, `or`, `not`을 사용할 수 있다.
- ⑤ 들여쓰기 수준이 올바른지 확인한다.

**선택:**

## Chapter 10. 반복해서 실행하기

### 57. [단답형]
다음 코드의 최종 `total` 값을 쓰시오.

```python
total = 0
for n in range(1, 4):
    total += n
```

**답:**

### 58. [다중선택]
반복문에 대한 설명으로 맞는 것을 모두 고르시오.

- ① `for`문은 반복할 값들을 하나씩 꺼내 처리할 수 있다.
- ② `while`문에서 상태가 변하지 않으면 무한 반복이 될 수 있다.
- ③ `continue`는 반복문 전체를 완전히 종료한다.
- ④ 반복문 안에 조건문을 함께 사용할 수 있다.
- ⑤ 중첩 반복문은 바깥 반복과 안쪽 반복의 실행 순서를 추적해야 한다.

**선택:**

## Chapter 11. 리스트로 여러 데이터 관리하기

### 59. [단답형]
다음 코드 실행 후 리스트를 쓰시오.

```python
nums = [1, 2]
nums.append(3)
print(nums)
```

**답:**

### 60. [다중선택]
리스트 상태를 확인하거나 변경하는 기능으로 맞는 것을 모두 고르시오.

- ① `len()` - 요소 개수 확인
- ② `in` - 값 포함 여부 확인
- ③ `pop()` - 요소 삭제 및 반환
- ④ `index()` - 값의 위치 찾기
- ⑤ `keys()` - 리스트의 모든 인덱스 반환

**선택:**

## Chapter 12. 튜플과 집합(set)

### 61. [단답형]
빈 집합을 만드는 파이썬 표현을 쓰시오.

**답:**

### 62. [다중선택]
상황에 맞는 자료구조 선택으로 적절한 것을 모두 고르시오.

- ① 값의 순서와 수정이 모두 중요하면 리스트를 고려한다.
- ② 한 번 정한 값의 조합을 직접 수정하지 않으려면 튜플을 고려한다.
- ③ 중복 제거가 중요하면 집합을 고려한다.
- ④ 중복을 반드시 유지해야 하는데 집합만 사용한다.
- ⑤ 집합은 교집합을 구하는 데 사용할 수 있다.

**선택:**

## Chapter 13. 딕셔너리로 키와 값 관리하기

### 63. [단답형]
없는 키를 조회할 때 KeyError를 피하기 위해 활용할 수 있는 딕셔너리 메서드 이름을 쓰시오.

**답:**

### 64. [다중선택]
다음 중 딕셔너리 처리로 맞는 것을 모두 고르시오.

- ① 새 키에 값을 대입해 항목을 추가할 수 있다.
- ② 기존 키에 새 값을 대입해 수정할 수 있다.
- ③ `keys()`는 키들을 확인하는 데 사용할 수 있다.
- ④ `values()`는 값들을 확인하는 데 사용할 수 있다.
- ⑤ 딕셔너리는 같은 키를 여러 개 독립 항목으로 그대로 유지한다.

**선택:**

## Chapter 14. 함수로 코드를 묶고 재사용하기

### 65. [단답형]
함수 정의에서 값을 받아들이는 이름을 무엇이라고 하는가?

**답:**

### 66. [다중선택]
함수 설계에 대한 설명으로 맞는 것을 모두 고르시오.

- ① 함수 이름은 역할이 드러나도록 작성하는 것이 좋다.
- ② 함수 안의 지역 변수는 일반적으로 함수 밖에서 그대로 사용할 수 없다.
- ③ 기본값 매개변수를 사용할 수 있다.
- ④ 키워드 인수로 인수 이름을 명시할 수 있다.
- ⑤ 모든 함수는 반드시 화면에 값을 print해야 한다.

**선택:**

## Chapter 15. 모듈·패키지와 가상환경

### 67. [단답형]
설치된 패키지 목록을 다른 환경에서 재현하기 위해 프로젝트에 기록하는 대표 파일 이름을 쓰시오.

**답:**

### 68. [다중선택]
`ModuleNotFoundError`가 발생했을 때 확인할 사항으로 적절한 것을 모두 고르시오.

- ① 모듈 파일의 위치와 이름
- ② 현재 선택된 Python 인터프리터
- ③ 패키지가 현재 가상환경에 설치되어 있는지
- ④ 무조건 모든 패키지를 전역 환경에 설치
- ⑤ VS Code가 프로젝트의 `.venv`를 사용하고 있는지

**선택:**

## Chapter 16. 파일과 CSV 데이터 다루기

### 69. [단답형]
기존 파일 내용 뒤에 새 내용을 추가할 때 사용하는 파일 모드 문자를 쓰시오.

**답:**

### 70. [다중선택]
CSV 데이터를 처리할 때 필요한 행동으로 맞는 것을 모두 고르시오.

- ① 열 이름을 기준으로 읽고 싶으면 DictReader를 고려한다.
- ② 숫자 열이 문자열이라면 계산 전 형 변환한다.
- ③ 파일 경로가 맞는지 확인한다.
- ④ FileNotFoundError가 나도 경로는 확인할 필요가 없다.
- ⑤ 반복문으로 여러 행을 처리해 합계나 평균을 계산할 수 있다.

**선택:**

## Chapter 17. 오류와 예외 처리

### 71. [단답형]
트레이스백을 처음 읽을 때 우선 확인하는 마지막 줄의 핵심 두 정보는 무엇인가?

**답:**

### 72. [다중선택]
다음 중 실행 중 예외의 예로 적절한 것을 모두 고르시오.

- ① `int("스물")`의 ValueError
- ② 0으로 나누기의 ZeroDivisionError
- ③ 없는 파일 열기의 FileNotFoundError
- ④ 콜론 누락으로 인한 SyntaxError
- ⑤ 없는 딕셔너리 키 접근의 KeyError

**선택:**

## Chapter 18. 클래스와 객체 기초

### 73. [단답형]
객체 생성 시 초기값을 저장하는 데 주로 사용하는 특별 메서드 이름을 쓰시오.

**답:**

### 74. [다중선택]
클래스 사용이 자연스러운 상황을 모두 고르시오.

- ① 같은 형태의 데이터와 관련 기능이 여러 객체에서 반복된다.
- ② 상품 객체마다 이름·가격·재고와 관련 메서드가 필요하다.
- ③ 단순한 값 한 개만 잠시 저장하면 반드시 클래스를 써야 한다.
- ④ 하나의 클래스로 여러 서로 다른 객체를 만들 수 있다.
- ⑤ 객체를 리스트에 넣어 반복 처리할 수 있다.

**선택:**

## Chapter 19. AI와 함께 프로그램 설계하고 작성하기

### 75. [단답형]
요구사항을 정한 다음 큰 기능을 역할별 작은 단위로 나눌 때 Chapter 19에서 권장한 대표 코드 단위를 쓰시오.

**답:**

### 76. [다중선택]
학생 점수 요약 프로그램을 설계할 때 코드 작성 전에 할 일로 적절한 것을 모두 고르시오.

- ① 저장할 데이터의 모양을 정한다.
- ② 입력·처리·출력을 구분한다.
- ③ 이번 실습에서 하지 않을 범위를 정한다.
- ④ 평균의 예상 결과를 작은 테스트 데이터로 미리 계산한다.
- ⑤ 설계 없이 AI가 만든 최종 코드부터 복사한다.

**선택:**
